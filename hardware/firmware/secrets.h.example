#pragma once

// Copy this file to secrets.h and fill in values from the Firebase Console.
// secrets.h is gitignored — never commit live credentials.

#define API_KEY "YOUR_FIREBASE_WEB_API_KEY"
#define FIREBASE_PROJECT_ID "YOUR_FIREBASE_PROJECT_ID"
#define USER_EMAIL "YOUR_DEVICE_SERVICE_ACCOUNT_EMAIL"
#define USER_PASSWORD "YOUR_DEVICE_SERVICE_ACCOUNT_PASSWORD"
